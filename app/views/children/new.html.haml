=render "layouts/tab"
=form_for :child, :url => {:action => :create} do |b|
  .center
    =b.label "Номер історії новонародженого"
    =b.number_field :history_id, placeholder: "рекомендовано: " + @last_h_c_id.to_s
    %br
    =b.label "Фамілія"
    =b.text_field :surname, value: @woman.surname
    %br
    =b.label "Імя"
    =b.text_field :fname
    %br
    =b.label "По-батькові"
    =b.text_field :sname
    %br
    =b.submit "Save"